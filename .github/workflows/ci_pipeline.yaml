name: CI\CD pipeline

on: [push]

jobs:
  CI-CD-Pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      
      - name: Build image
        run: |
          docker compose build

      - name: Unit tests
        run: |
          python --version
          

        